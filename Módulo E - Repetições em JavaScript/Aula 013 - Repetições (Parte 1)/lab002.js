// ESTRUTURA DE REPETIÇÃO DO WHILE

/*
WHILE -> estrutura de repetição com teste lógico no início (testa depois faz)
DO WHILE -> estrutura de repetição com teste lógico no final (faz depois testa)
*/
var contador = 1;
do {
    console.log(contador);
    contador = contador + 1;
} while (contador <= 10)